[build]
builder = "nixpacks"



[[services]]
name = "backend"
root = "backend"
buildPath = "backend"

[services.build]
builder = "docker"
dockerfilePath = "Dockerfile"

[[services]]
name = "frontend"
root = "frontend"
buildPath = "frontend"
buildCommand = "npm run build"
startCommand = "npm run preview -- --host --port ${PORT:-4173}"

[services.build]
builder = "nixpacks"
